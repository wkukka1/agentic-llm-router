name: 10_finetune_minilm_qonly
notes: >
  Ablation of experiment 07 on question-only prompts. Same purpose as 09, for
  the fine-tuned family.
model:
  name: finetune_transformer
  params:
    model_name: sentence-transformers/all-MiniLM-L6-v2
    max_length: 256
    batch_size: 32
    epochs: 5
    lr: 0.00005
data:
  variant: question_only
