name: 08_finetune_bge_small
notes: >
  Full fine-tune of bge-small-en-v1.5 (33M params, 12 layers). Expected ceiling
  of the sweep; the question is what it costs in latency over 07.
model:
  name: finetune_transformer
  params:
    model_name: BAAI/bge-small-en-v1.5
    max_length: 256
    batch_size: 32
    epochs: 5
    lr: 0.00003
data:
  variant: full_prompt
