"""Serving-side data representation: :mod:`router.data.nirt` (``NIRTDataset``).

The rest of the Phase 0 data layer -- loaders, canonical schemas,
normalization, response matrix, chance correction, splits, quality checks,
the Phase 1 facade -- is training-only and lives in :mod:`training.data`."""
